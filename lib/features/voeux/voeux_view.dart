import 'package:flutter/material.dart';

class VoeuxView extends StatelessWidget {
  const VoeuxView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Voeux")));
  }
}
